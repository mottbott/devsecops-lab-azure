# Local Claude guide — intentionally not versioned
CLAUDE.md

# Working docs — all of docs/ is local-only, never check in
docs/

# Terraform
.terraform/
.terraform.lock.hcl.backup
*.tfstate
*.tfstate.*
*.tfplan
tf.plan
tf-apply.log
crash.log
crash.*.log
# Variable files may contain secrets — commit a *.tfvars.example instead
*.tfvars
*.tfvars.json
!*.tfvars.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment / secrets — both dotfile and non-dotfile names
.env
.env.*
env
env.*
!.env.example
!env.example

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
